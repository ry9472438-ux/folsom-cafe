import { Minus, Plus, ShoppingBag } from 'lucide-react';
import { useOrder } from '@/context/OrderContext';
export default function OrderSummary(){const {items,change,total}=useOrder();return <div><h2 className="flex items-center gap-3 font-heading text-3xl font-bold"><ShoppingBag className="text-[#D4AF37]"/>Your order</h2><div className="mt-8 space-y-4">{!items.length&&<p className="text-[#E5E5E1]/50">Your order is empty. Add something delicious from the menu.</p>}{items.map(item=><div key={item.name} className="flex items-center justify-between border-b border-white/10 pb-4"><div><b>{item.name}</b><p className="text-sm text-[#D4AF37]">Rs. {(item.price*item.qty).toLocaleString()}</p></div><div className="flex items-center gap-3 rounded-full border border-white/10 p-1"><button onClick={()=>change(item.name,-1)} className="p-1"><Minus size={15}/></button><span className="min-w-5 text-center text-sm">{item.qty}</span><button onClick={()=>change(item.name,1)} className="p-1"><Plus size={15}/></button></div></div>)}</div>{items.length>0&&<div className="mt-7 flex justify-between font-heading text-2xl font-bold"><span>Total</span><span className="text-[#D4AF37]">Rs. {total.toLocaleString()}</span></div>}</div>}