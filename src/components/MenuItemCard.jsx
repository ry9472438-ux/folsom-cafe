import { Plus } from 'lucide-react';
import { useOrder } from '@/context/OrderContext';
export default function MenuItemCard({item}){const {add}=useOrder();return <article className="flex min-h-40 flex-col justify-between rounded-2xl border border-white/10 bg-[#181818] p-5 transition hover:border-[#D4AF37]/40"><h3 className="font-heading text-xl font-bold leading-7">{item.name}</h3><div className="mt-7 flex items-center justify-between"><span className={item.price?'font-semibold text-[#D4AF37]':'text-xs text-[#E5E5E1]/40'}>{item.price?`Rs. ${item.price.toLocaleString()}`:'Ask our team'}</span>{item.price&&<button onClick={()=>add(item)} aria-label={`Add ${item.name}`} className="grid h-10 w-10 place-items-center rounded-full border border-[#D4AF37]/60 text-[#D4AF37] transition hover:bg-[#D4AF37] hover:text-black"><Plus size={18}/></button>}</div></article>}